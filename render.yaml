services:
  - name: frontend
    type: web
    buildCommand: ./images/frontend
    startCommand: npm start
    envVars:
      - key: BACKEND_URL
        fromService:
          name: backend
          property: host:4000

  - name: backend
    type: web
    buildCommand: ./images/backend
    startCommand: npm start

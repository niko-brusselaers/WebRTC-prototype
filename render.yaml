services:
  - name: frontend
    type: web
    buildCommand: ./images/frontend
    startCommand: npm start
    envVars:
      - key: BACKEND_URL
        fromService:
          name: backend
          property: url  # Change 'property' to the appropriate property of the backend service

  - name: backend
    type: web
    buildCommand: ./images/backend
    startCommand: npm start
